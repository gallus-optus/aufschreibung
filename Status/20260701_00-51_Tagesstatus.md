# Tagesstatus — 01.07.2026 (00:51 MESZ)

## Gesamtstand in einem Satz

In dieser Sitzung wurden **Phase C (Tageseingabe) abgeschlossen und auf
dem Handy bestätigt** sowie **Phase F (Dropbox-Zwei-Wege-Sync) komplett
gebaut und lokal verifiziert** — Phase F wartet jetzt auf den Handy-Test.

---

## Heute erledigt

### Phase B2 final abgehakt
- Handy-Test war erfolgreich (Nutzer-Bestätigung), Repo öffentlich,
  GitHub Pages aktiv. Tagesstatus committet und gepusht.

### Neue Projektregel (CLAUDE.md)
- Workflow-Regel 7 ergänzt: Vor jeder Aufgabe schlägt der Coder Modell
  und Aufwand vor und wartet auf „weiter".

### Phase C — Tageseingabe (gebaut, getestet, ein Fix)
- Fünf neue Bildschirme: Heute (Kalorien-Donut, Trinkbalken,
  Mahlzeiten), Erfassen-Suche, Menge+Einheit, Einheiten-Popup,
  Vitalwerte. Navigation auf 5 Tabs erweitert; B2-Listen unter
  „Datenbank", Einstellungen unter „Mehr".
- Einheiten-Logik (Gramm/Stück/Portion/Scheibe/Esslöffel/Teelöffel) mit
  einmaliger Gramm-Abfrage; Bearbeiten/Löschen von Einträgen.
- IndexedDB auf v2 migriert (ohne B2-Daten zu zerstören), alle
  Datensätze sync-bereit (`geaendert_am`/`sync_status`).
- **Handy-Test bestanden.** Ein gemeldeter Mangel behoben: Uhrzeit-Feld
  ist jetzt natives `type="time"` (Android-Tastatur hatte keinen
  Doppelpunkt).

### Phase F — Dropbox-Sync (gebaut, lokal verifiziert)
- Schreibender Zwei-Wege-Sync: herunterladen → zeilenweise mergen →
  Backup → hochladen. Rollierendes Backup (5 Versionen in `_backups/`),
  rev-Tracking, Auslöser App-Start + 60-s-Debounce + manueller Knopf,
  Sperre gegen Überlappung, Offline/Token-Erneuerung.
- Robuster CSV-Parser (RFC-4180-Quoting) + deutscher Serialisierer
  (Semikolon, Komma-Dezimal, UTF-8-BOM).
- Mehr-Sektion zeigt Sync-Status und Sicherungen (wie Mockup).
- **Lokal mit simulierter Dropbox vollständig getestet:** Erst-Upload +
  Header-Migration, Zwei-Geräte-Merge ohne Verlust, Konflikt nach
  Zeitstempel, Backup-Rotation (7→5), Offline, Idempotenz, CSV-Quoting.
  Null Konsolenfehler.

### Sonstiges
- Drei Phase-C-Commits und drei Phase-F-Commits gepusht.
- Mockups für C und F ins Repo (`Mockups/`), Bauaufträge dokumentiert
  (inkl. `mittags_start` und CSV-Schema-Erweiterungen).
- Letzter gepushter Stand: Commit `545a93b`.

---

## Offene Punkte / Blocker

| Punkt | Status | Wer |
|-------|--------|-----|
| **Manuelle Sicherung des Dropbox-Ordners vor dem ersten F-Sync** | offen — Sicherheits-Vorsichtsmaßnahme | Nutzer |
| **Handy-Test Phase F** (Backups, Zwei-Geräte, Excel öffnet CSV) | wartet | Nutzer |
| Handy-Test Phase C | erledigt — bestanden | — |

---

## Geklärte Entscheidungen (Phase F)

- **Konflikt-Zeitstempel:** `server_modified` der Dropbox-Datei als
  Ersatz (`geaendert_am` kommt nicht in die CSV). Lokal gewinnt, wenn
  `geaendert_am >= server_modified`.
- **02 (denormalisiert):** nur `gramm_pro_*` propagieren, je Gericht auf
  der ersten Zutat-Zeile; Zutaten gelten als von Dropbox autoritativ.
- **`ist_eigengericht`** als `true`/`false`; **05_Konfiguration** nur
  lesend (kein Backup).
- **Kein Tombstone:** Löschungen propagieren nicht über Geräte (bekannte
  Einschränkung).

---

## Nächste Schritte

1. **Nutzer:** Dropbox-Ordner einmal manuell sichern.
2. **Nutzer:** App neu laden (Cache v0.3), Testplan
   `Status/20260701_00-46_Testplan_PhaseF.md` durchgehen — Schwerpunkt
   Zwei-Geräte-Test und Backup-Prüfung.
3. **Coder:** gemeldete Fehler fixen — oder bei grünem Test Phase D
   (Rechner-Tools) vorbereiten.

---

## Was lokal NICHT getestet werden konnte

Der echte Dropbox-Round-Trip (Upload/Copy/Delete gegen die Live-API)
sowie Auth laufen nur über die GitHub-Pages-URL auf dem Handy. Lokal
wurde die gesamte Sync-Logik mit einer simulierten In-Memory-Dropbox
(gestubbte I/O) durchgetestet — die Korrektheit von Merge, Backup-
Rotation und CSV-Serialisierung ist damit abgesichert, der reale
Netzwerk-Teil bleibt dem Handy-Test vorbehalten.
